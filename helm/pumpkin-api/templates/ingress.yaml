apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pumpkin-api
  spec:
    ingressClassName: nginx
spec:
  rules:
    - host: pumpkin.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pumpkin-api
                port:
                  number: 80