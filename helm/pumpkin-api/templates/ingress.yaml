apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pumpkin-api
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: pumpkin.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pumpkin-api
                port:
                  number: 80